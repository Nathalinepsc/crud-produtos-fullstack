@model List<ProjetoNsaSenhora.Web.Models.ProdutoViewModel>
@{
    ViewData["Title"] = "Listagem de Produtos";
}
<h1>Produtos</h1>

<a asp-action="Create" class="btn btn-primary mb-3">Adicionar Produto</a>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nome</th>
            <th>Descrição</th>
            <th>QuantidadeEstoque</th>
            <th>PrecoUnitario</th>
            <th>ValorTotalEstoque</th> 
            <th>DataCadastro</th>   
            <th>PercentualDesconto</th>
            <th>StatusCategoria</th>
            <th>Ativo</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var produto in Model)
        {
            <tr>
                <td>@produto.Id</td>
                <td>@produto.Nome</td>
                <td>@produto.Descricao</td>
                <td>@produto.QuantidadeEstoque</td>
                <td>@produto.PrecoUnitario.ToString("C")</td>
                <td>@produto.ValorTotalEstoque.ToString("C")</td>
                <td>@produto.DataCadastro.ToLocalTime().ToString("dd/MM/yyyy HH:mm")</td>
                <td>@produto.PercentualDesconto</td>
                <td>@produto.StatusCategoria.ToString()</td>
                <td>@produto.Ativo</td>
            </tr>
        }
    </tbody>
</table>
